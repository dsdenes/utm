{
  "name": "utm",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src/ -d build && browserify build/index.js -o lib/bundle.js && uglifyjs lib/bundle.js -o lib/bundle.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "browserify": "^14.4.0",
    "uglify-js": "^3.0.26"
  },
  "dependencies": {
    "query-string": "^5.0.0"
  }
}
